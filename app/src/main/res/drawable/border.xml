<shape xmlns:android="http://schemas.android.com/apk/res/android">
  <solid android:color="@android:color/transparent"/> <!-- Background color -->
  <stroke android:width="1dp" android:color="?attr/colorOnPrimary"/> <!-- Border width and color -->
  <corners android:radius="9dp"/> <!-- Rounded corners (optional) -->
</shape>